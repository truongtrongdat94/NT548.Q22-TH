apiVersion: v1
kind: Service
metadata:
  name: api-preview-svc
  labels:
    app: api
    role: preview
spec:
  type: ClusterIP
  selector:
    app: api
  ports:
    - name: http
      protocol: TCP
      port: 4000
      targetPort: 4000
